namespace Loader
{
    public class LoaderData
    {
        public LoaderData(float progress)
        {
            Progress = progress;
        }

        public float Progress { get; set; }
    }
}
